import React from 'react';
import IframeWidget from "../../widgets/IframeWidget/IframeWidget";



function IframeList(props) {
    const {iframesData} = props;
    // TODO add paper and toggle header to Iframewidget. Must then pass help function. You could also add
    // name to toggle header. When you are done, pass the props here as approiately
    return (
        <div>
            {/*{urlList.map(url => <IframeWidget />)}*/}
        </div>
    );
}

export default IframeList;